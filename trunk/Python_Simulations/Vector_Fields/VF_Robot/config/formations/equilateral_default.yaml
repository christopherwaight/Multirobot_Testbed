# Formation configuration: Default equilateral triangle
# This configuration matches the original off_size = 0.25 used in robot_cluster.py
#
# SAS parameterization: (p, q, beta)
# p: distance from robot 1 to robot 2
# q: distance from robot 2 to robot 3
# beta: angle at robot 2 (in degrees)

formation:
  p: 0.33   # side length (m); matches the 0.33 m hardware formation in the paper
  q: 0.33
  beta_degrees: 60.0

  # Formation control gains
  position_gain: 0.5    # Proportional gain for p and q error correction (intermediate value)
  angle_gain: 0.01      # Proportional gain for beta error (much lower for stability)
